@@ -247,7 +249,7 @@
               </div>
               <div className="px-4 py-2 text-center text-sm">
                 <div className="font-light">
-                  {formatPeriodLabel(periodDescriptor as any)}
+                  {formatPeriodLabel(periodDescriptor)}
                 </div>
                 {gameScheduleState !== 'PPD' && gameState !== 'OFF' && gameState !== 'FINAL' && (
                   <GameClock clock={clock} gameState={gameState} />
